import SupportPage from '@/features/support/components/support-page';

export const metadata = {
  title: 'Dashboard : Support & Dokumentation'
};

export default function Page() {
  return <SupportPage />;
}
